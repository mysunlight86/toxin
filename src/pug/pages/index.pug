extends ../layout/main

block variables
  - var title = 'Toxin';
  - var description = 'Сайт о бронировании номера в гостинице';
  - var pageClass = 'index-page';

block content
  section
    .container
      h1 Главная страница
      p Первый абзац

block scripts
  script(type='text/javascript').
    const foo = true;
    let bar = function() {};
    if (foo) {
    bar(1 + 5)
    }
